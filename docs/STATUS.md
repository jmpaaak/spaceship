# STATUS
- preflight this cycle: PASS (engine tests+package PASS, git diff check PASS). No uncommitted prior-cycle work to preserve (only an unrelated pre-existing `loop/env.sh` WAIT_SECONDS default tweak, untouched, already tracked by a prior commit).
- Slice: item14(C) `rerollBonus` gap, one level deeper than the previously-documented "run wiring" — `M.spendReroll(run)` only ever decremented a per-expedition counter with no effect of its own, and `M.rollGearOffer(run, pool, rolls)` only ever generated a gear offer with no gate on an actual reroll budget. The two halves of "spend a free reroll to get a new gear offer" existed but were never atomically joined — exactly the "counter exists but nothing spends it for its documented effect" class of gap this lane previously closed for `boostCharge` (`M.spendBoost`) and `insurance` (`M.damage`'s one-shot gate).
- Fix: new `game/expedition.lua` function `M.rerollGearOffer(run, pool, rolls)` — refuses (`false` + error-message, no state mutated) when `M.rerollsRemaining(run)` is zero, otherwise spends exactly one free reroll via the existing `M.spendReroll` and returns `true` + the freshly generated offer from the existing `M.rollGearOffer`. Same reject-don't-partial-apply contract as every other atomic run mutator in this file.
- TDD: `game/self_test.lua`'s new `testGearRerollOfferSpendWiring()` (RED confirmed: `attempt to call field 'rerollGearOffer' (a nil value)`, then GREEN) covers: refusal with zero rerolls remaining (no `rerollsUsed` mutation), a successful call with exactly one `rerollBonus` card returning a real offer table and decrementing `rerollsRemaining` from 1 to 0, and a second call refusing once the budget is exhausted (no offer leaked, no further decrement).
- `make test`(GAME_HEADLESS=1 GAME_UNIT=1 love .) and `make verify LOVE=/Users/jm/.local/bin/love` both GREEN (`SPACESHIP_UNIT_OK`, `SPACESHIP_SMOKE_OK` x3, `LOVE_BUNDLE_OK:build/game.love:58`, `ASSET_MANIFEST_OK`).
- Changed files: `game/expedition.lua`, `game/self_test.lua`, `docs/GEAR_SCHEMA.md`, `docs/STATUS.md`, `docs/feedback/INBOX.md`. `play.lua`/`i18n.lua`/`world.lua`/`game/gear.lua`/`game/engine_parts.lua`/`game/data/*.json` untouched (`git status --short` confirmed) — this slice is purely the "wire an existing pure/data-layer building block into run consumption" exception this lane's `loop/PROMPT.md` permits, applied to `game/expedition.lua`'s own two pre-existing functions rather than to `game/gear.lua`.
- Next slice: item13->9->10->12->14 residual gap re-audit (this lane's recurring pattern) — a broad sweep of `hull_parts.json`/`engine_parts.json` for the "slot-scope dead card" and "content coverage" classes of gap found nothing new this cycle (checked: engine cards with only hull-only effect types, hull cards with only engine-only effect types, category-agnostic effect-type coverage in the engine pool, edition coverage in both pools, `galaxyExclusive` coverage in both pools — all clean). The actual shop/checkpoint UI tap affordance that would call the new `M.rerollGearOffer` (and `M.buyGearFromShopPlanet`/`M.exploreHub`/`M.spendBoost` from earlier slices) remains `play.lua`/`world.lua` territory and out of this lane's scope.

> 이전 cycle 이력은 `docs/STATUS_HISTORY.md`에 있다. 특정 과거 버그를 추적할 때만 그 파일을 검색하고, 평소에는 읽지 않는다.
