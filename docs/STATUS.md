# STATUS
- preflight this cycle: PASS (engine tests+package PASS, git diff check PASS). No uncommitted prior-cycle work to preserve (only an unrelated `loop/env.sh` WAIT_SECONDS default tweak was pre-existing and untouched).
- Slice: item7(a) gap — the shop-planet money-purchase acquisition path (`world.shopPlanet(galaxy)`, a real deterministic coordinate since an earlier slice) had no run function a shop-planet encounter could actually call.
- Gap: item 9(c)'s `M.buyGear` is the only existing "spend money to occupy a gear slot" function, but it is hard-gated to `run.phase == "settlement"` (the Earth shop) and explicitly refuses `galaxyExclusive` parts (item 7(c)'s Earth-only exclusion rule). Item 7(a)'s text ("각 은하계의 고정 좌표에 존재하는 상점 행성에서 돈으로 구매") describes a DIFFERENT, in-flight acquisition path with no restriction on galaxy-exclusive stock — but nothing in `game/expedition.lua` let a mid-flight shop-planet visit spend money on gear. `world.shopPlanet`'s coordinate generation and `gearModule.buyPrice`/`M.equipGear`'s price/equip mechanism both already existed in isolation; the connecting run function was the missing piece, the same "documented acquisition path with zero consumers" class of gap this lane has repeatedly found and closed for individual effect types.
- Fix: new `game/expedition.lua` function `M.buyGearFromShopPlanet(run, category, part)` — gated to `run.phase == "ascending"` (refuses during `settlement` too, since that is the distinct `M.buyGear` Earth-shop path, not an alias), same atomic no-partial-apply contract (`true, price` / `false, error-message`, never a partial deduction/equip), uses `M.shopPrice` (item 14(F) `shopDiscount` gear applies) and `M.equipGear` (hull/engine slot independence + immediate `refreshShipStats` preserved). Unlike `M.buyGear`, it does NOT reject `galaxyExclusive` parts, since only item 7(c) names Earth's restriction and a shop planet's whole reason to exist is a physical in-galaxy location that may legitimately stock that galaxy's own exclusive card.
- TDD: `game/self_test.lua`'s new `testGearShopPlanetPurchaseWiring()` (RED confirmed by temporarily renaming the new function to `buyGearFromShopPlanetDISABLED` and re-running `GAME_HEADLESS=1 GAME_UNIT=1 love .`, which failed with `attempt to call field 'buyGearFromShopPlanet' (a nil value)` at the new test; reverted, then GREEN) covers: refusal outside `ascending` (including explicitly during `settlement`, proving this is not `M.buyGear` under another name) with money/slots unchanged, a successful `ascending`-phase purchase with exact price deduction and immediate `maxDurability` refresh, insufficient-money refusal without mutation, a successful `galaxyExclusive` purchase (the behavioral difference from `M.buyGear`), an equipped `shopDiscount` card reducing the price identically to `M.buyGear`, and an engine-slot purchase staying independent of the hull slot list (item 10 category isolation).
- `make test`(GAME_HEADLESS=1 GAME_UNIT=1 love .) and `make verify LOVE=/Users/jm/.local/bin/love` both GREEN (`SPACESHIP_UNIT_OK`, `SPACESHIP_SMOKE_OK` x3, `LOVE_BUNDLE_OK:build/game.love:58`, `ASSET_MANIFEST_OK`).
- Changed files: `game/expedition.lua`, `game/self_test.lua`, `docs/GEAR_SCHEMA.md`, `docs/STATUS.md`, `docs/feedback/INBOX.md`. `play.lua`/`i18n.lua`/`world.lua`/`game/gear.lua`/`game/engine_parts.lua` untouched (`git status --short` confirmed); the `game/expedition.lua` change is exactly the "wire an existing pure/data-layer building block into run consumption" exception this lane's `loop/PROMPT.md` permits.
- Next slice: item13->9->10->12->14 residual gap re-audit (this lane's recurring pattern), or item7's remaining actual UI consumption point (an in-flight prompt near `world.shopPlanet` that calls the new `M.buyGearFromShopPlanet` with a real card offer, `play.lua`/`world.lua` territory and out of this lane's scope).

> 이전 cycle 이력은 `docs/STATUS_HISTORY.md`에 있다. 특정 과거 버그를 추적할 때만 그 파일을 검색하고, 평소에는 읽지 않는다.
