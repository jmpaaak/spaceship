# STATUS
- preflight this cycle: PASS (engine tests+package PASS, git diff check PASS). No uncommitted prior-cycle work to preserve (only an unrelated pre-existing `loop/env.sh` WAIT_SECONDS default tweak, untouched, already tracked by a prior commit).
- Slice: item13/14 web-editor gap — every other numeric axis `game/gear.lua` owns (effect value range, rarity/edition whitelists, edition effect transforms) already has a byte-for-byte sync check against `tools/gear-editor/editor.js`, but item 9(c)/12's sell-to-rebuy economy constants (`M.raritySellValue`, `M.editionSellBonus`, `M.buyPriceMultiplier`, and the `M.sellValue`/`M.buyPrice` combination formulas) had zero editor counterpart — an author designing a new card in the web editor had no way to see its actual sell/buy price without reading `game/gear.lua` by hand, despite item 14's own "웹 에디터 폼 동기화" mandate.
- Fix: `tools/gear-editor/editor.js` gains `RARITY_SELL_VALUE`/`EDITION_SELL_BONUS`/`BUY_PRICE_MULTIPLIER` constants plus pure `computeSellValue(rarity, editionId)`/`computeBuyPrice(rarity, editionId)` functions replicating `gear.lua`'s exact combination order (rarity base × edition `sellMultiplier` if any, then + the flat `editionSellBonus`, then × 3 for buy price). New `updateEconomyPreview()` renders a live "no edition" baseline plus one row per listed edition (`sell $N · buy $N`) into a new `economyPreviewContainer` (`tools/gear-editor/index.html`, reusing the existing `.edition-preview` CSS class), wired into `openForm`/rarity-change/editions-input the same way `updateEditionPreview` already is.
- TDD: `game/self_test.lua`'s new `testGearEditorEconomyPreviewSync()` locks the three constants against `gear.lua`, confirms `updateEconomyPreview` is defined and actually called (not dead code), confirms `economyPreviewContainer` exists in `index.html`, and confirms `computeSellValue` applies an edition's `sellMultiplier` (catching a wrong combination order, not just a wrong constant). RED confirmed by temporarily setting `EDITION_SELL_BONUS = 99` in `editor.js` (assertion: "editor.js EDITION_SELL_BONUS must equal gear.lua's M.editionSellBonus (6), got 99"), reverted, GREEN restored.
- Side-fix (required to keep tests running at all): adding this 5th gear-editor sync test pushed `game/self_test.lua`'s `M.run()` past Lua's 60-upvalue-per-function ceiling (`function at line 3741 has more than 60 upvalues`, a syntax error, not an assertion failure) — this suite has accumulated enough local test functions across many prior cycles that the flat list of calls in `M.run()` itself became the bottleneck. Fixed by grouping the five gear-editor sync test calls (`testGearEditorEditionAndRaritySync`, `testGearEditorEditionEffectPreviewSync`, `testGearEditorEffectValueRangeSync`, the new `testGearEditorEconomyPreviewSync`, `testGearEditorGalaxyExclusiveFieldSync`) behind one `testGearEditorSyncSuite()` wrapper function — `M.run()` now only references that one upvalue instead of five. No test behavior changed, only how the calls are grouped; all five still run in the same order.
- `make test`(GAME_HEADLESS=1 GAME_UNIT=1 love .) and `make verify LOVE=/Users/jm/.local/bin/love` both GREEN (`SPACESHIP_UNIT_OK`, `SPACESHIP_SMOKE_OK` x3, `LOVE_BUNDLE_OK:build/game.love:58`, `ASSET_MANIFEST_OK`).
- Changed files: `tools/gear-editor/editor.js`, `tools/gear-editor/index.html`, `game/self_test.lua`, `docs/GEAR_SCHEMA.md`, `docs/STATUS.md`, `docs/feedback/INBOX.md`. `play.lua`/`i18n.lua`/`world.lua`/`game/gear.lua`/`game/engine_parts.lua`/`game/expedition.lua` untouched (`git status --short` confirmed) — this slice is pure web-editor content (item 13's own scope) plus a test-suite-organization refactor required to keep the suite runnable, no gameplay/run-wiring code touched.
- Next slice: item13->9->10->12->14 residual gap re-audit (this lane's recurring pattern). A broad sweep this cycle (every `gear.lua`/`engine_parts.lua` exported function's call sites in `expedition.lua`, every `game/data/*.json` effect-type/edition/galaxyExclusive coverage, every editor.js constant/table against its `gear.lua` source) found nothing else unwired or un-synced. The remaining candidates are all explicitly documented as deferred UI work requiring `game/scenes/play.lua`/`game/world.lua` (out of this lane's scope): the actual EARTH SHOP/shop-planet/hub-drop tap affordances for `M.buyGear`/`M.sellGear`/`M.buyGearFromShopPlanet`/`M.exploreHub`/`M.rerollGearOffer`/`M.spendBoost`, and the in-flight collision/detection-radius/auto-collect call sites for `M.collisionRadius`/`M.detectionRadius`/`M.autoCollectEnabled`.

> 이전 cycle 이력은 `docs/STATUS_HISTORY.md`에 있다. 특정 과거 버그를 추적할 때만 그 파일을 검색하고, 평소에는 읽지 않는다.
