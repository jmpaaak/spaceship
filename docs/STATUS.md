# STATUS
- 이번 사이클 시작 시 uncommitted diff 발견: 전 사이클이 `game/expedition.lua`/`game/self_test.lua`에 항목10(b)/9 gap 배선을 이미 작성해 두고 커밋하지 않은 상태였다 — 워크플로 규칙("prior-cycle work를 보존하고 완료하라")에 따라 그대로 검증 후 완료했다(재작성 없음).
- gap 내용: 항목10의 원문이 "상승 가속"을 엔진 부품 특화 효과로 명시하고, 실제로 번들 `engine_parts.json`(24종) 중 7종(`engine_afterburner`, `engine_fusion_core`, `engine_ion_drive`, `engine_ember_burst_valve`, `engine_void_phase_thruster`, `engine_solar_sail_flap`, `engine_singularity_drive`)이 `climbSpeed` 효과를 갖지만, 상승 속도를 계산하는 유일한 함수 `expedition.effectiveClimbSpeed(run)`가 지금까지 `run.equippedGear`(hull 전용)만 읽어 엔진 슬롯의 `climbSpeed` 값은 검증/로드/시너지 태그까지 다 됐음에도 실제 상승 속도에는 전혀 반영되지 않는 죽은 콘텐츠였다 — 이 레인이 반복 적용해온 "문서화됨-로드됨-읽히지 않음" 패턴의 잔여 gap.
- 수정: `game/expedition.lua`의 `M.effectiveClimbSpeed(run)`가 `gearModule.aggregateEffects(run.equippedEngineParts or {}).climbSpeed`를 추가로 더하도록 변경했다. 항목9가 태그-시너지 콤보 배율을 명시적으로 hull("선체(조커형)") 부품 범위로 한정하므로, 엔진 슬롯의 climbSpeed는 `equippedTotals`(시너지 배율 포함)가 아니라 `aggregateEffects`(순수 가산)로 별도 가산했다 — hull의 시너지-배율 총합에 엔진의 평범한 가산 보너스가 나란히 더해지는 구조.
- TDD: `game/self_test.lua`에 `testGearEngineClimbSpeedRunWiring()`을 추가했다 — (1) 미장착 기본 run은 `climbSpeed` 그대로(회귀 가드), (2) 엔진 슬롯에 `climbSpeed +5` 카드 장착 시 20→25로 정확히 가산, (3) hull `climbSpeed +7` + engine `climbSpeed +5`를 동시 장착 시 base 20 위에 두 기여가 모두 가산되어 32(hull은 시너지 배율 대상이지만 단일 카드라 공유 태그 쌍이 없어 x1, engine은 항상 순수 가산이므로 20+7+5=32).
- `make test`(GAME_HEADLESS=1 GAME_UNIT=1 love .)와 `make verify LOVE=/Users/jm/.local/bin/love` 모두 GREEN(`SPACESHIP_UNIT_OK`, `SPACESHIP_SMOKE_OK` x3, `LOVE_BUNDLE_OK:build/game.love:58`, `ASSET_MANIFEST_OK`).
- 변경 파일: `game/expedition.lua`(엔진 슬롯 climbSpeed 가산 배선 1개 함수), `game/self_test.lua`(신규 회귀 테스트 1개 + `M.run()` 등록). `game/gear.lua`/`game/engine_parts.lua`/`play.lua`/`i18n.lua`/`world.lua` 미변경.
- 다음 슬라이스: 항목13→9→10→12→14 잔여 gap 재감사. 후보: (a) 엔진 슬롯 climbSpeed와 유사하게 다른 순수-가산 (A)/(G) 효과 중 hull/engine 비대칭 소비자가 남아있는지 전수 재확인, (b) crystallized/quantum_flawed 등 에디션 수치를 실제로 노출하는 상점 UI(`play.lua`, 다른 레인 소관 — 이 레인은 데이터/로직 계층까지만), (c) 항목14 나머지 효과 스키마 그룹의 카드x카테고리 조합 커버리지 재감사.

> 이전 cycle 이력은 `docs/STATUS_HISTORY.md`에 있다. 특정 과거 버그를 추적할 때만 그 파일을 검색하고, 평소에는 읽지 않는다.
